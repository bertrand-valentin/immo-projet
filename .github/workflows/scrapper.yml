name: 🏗️ Notion Scraper

on:
  workflow_dispatch:
    inputs:
      url:
        description: "URL de la carte à scrapper"
        required: true
        default: ""
      pageId:
        description: "ID de la page Notion à mettre à jour"
        required: true
        default: ""

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout du code
        uses: actions/checkout@v4

      - name: ⚙️ Configuration Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Installation des dépendances
        run: npm ci

      - name: 🚀 Lancement du scraper
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DB_ID: ${{ secrets.NOTION_DB_ID }}
          MAKE_WEBHOOK_URL: ${{ secrets.MAKE_WEBHOOK_URL }}
        run: |
          node src/main.js ${{ github.event.inputs.url }} ${{ github.event.inputs.pageId }}